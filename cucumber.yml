# Profile default (cucumber sem tags)
default: -p html -p json

# Profiles
ci: -p json
html: --format pretty --format html --out=reports/html/scenario.html
json: --format pretty --format json --out=reports/json/scenario.json
